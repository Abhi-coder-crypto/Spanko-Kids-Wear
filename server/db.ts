// Database connection is disabled for now as requested
// keeping the file to avoid import errors in other files that might reference it
// though currently only storage.ts likely referenced it and we just updated storage.ts to remove db dependency.

export const db = null;
export const pool = null;
