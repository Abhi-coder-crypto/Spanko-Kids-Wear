## Packages
framer-motion | Essential for smooth page transitions and micro-interactions
embla-carousel-react | For the hero banner and product sliders
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes efficiently

## Notes
- Images will use Unsplash placeholders where dynamic data isn't available yet.
- Backend API endpoints are defined in shared/routes.ts and must be matched exactly.
- The design uses a playful, kid-friendly aesthetic with 'Fredoka' and 'DM Sans' fonts.
